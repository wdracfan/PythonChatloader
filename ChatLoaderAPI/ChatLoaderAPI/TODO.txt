TODO & ANSWERS:

1) название схемы в базе данных postgres? (параметр tableName где-то в коде)

2) какая connection string?

3) какие конкретно запросы может принимать api? скачать сообщения по номеру - один номер или диапазон?
скачать по дате - одна дата или диапазон? я пока сделал один номер, но диапазон дат, потом если что поменяю

адреса ручек в url тоже надо как-то согласовать

4) для удобства введения даты разбил дату на год-день-месяц (отдельные окошки в сваггере), но тогда ломается url
либо мне надо будет собирать url руками, либо тебе распаршивать то, что есть
пример: /ChatLoader/download/date?channelHandle=fghj&StartDate.Year=1&StartDate.Month=142&StartDate.Day=1&EndDate.Year=1&EndDate.Month=1&EndDate.Day=1
NB мне: сделать адекватную обработку неверных дат, а то он на дату 1.142.1 не поругался))

5) к колонке id нужно добавить identity, чтобы счётчик сам увеличивался:

-- auto-generated definition
create table requests
(
    id  integer generated always as identity
        constraint id
            primary key,
    url           text      not null,
    request_time  timestamp not null,
    response_time timestamp,
    answer        text,
    status        integer
);

alter table requests
    owner to postgres;
    
6) вопрос с тем, как (и по каким входным данным от пользователя) по-умному доставать ответ (ручка show - пока она пустая)

7) добавить документацию в сваггер

8) исправить Dockerfile, а то Rider, скорее всего, опять склепал неправильный
