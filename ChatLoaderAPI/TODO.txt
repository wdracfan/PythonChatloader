TODO & QUESTIONS:

В контроллере константа UrlStart - начало url. 
Если принципиально, на что начинается url, то там нужно будет поменять.

В xml-комментах и в description для swagger'а пока просто заглушки, 
со временем можно будет туда написать что-то содержательное.

Ручки show-messages и get-all-channnels пишут в базу запрос, потом ждут секунду (в dev окружении 10 секунд)
и выдают значение поля answer из той же строки. Мб не слишком умно, но пока так

Очень надеюсь, что не будет проблем с подключением к базе данных...

Даты в swagger вводятся в формате dd.MM.yyyy hh:mm. Некорректные даты не разрешает вводить сам swagger,
(!)за исключением времени 24:00(!). Пока это не пофикшено (код статус всё равно 400, но не от меня, а от swagger
по причине ошибки валидации), но если нужно фиксить, то надо будет подумать.
