TODO & QUESTIONS:

В контроллере константа UrlStart - начало url. 
Если принципиально, на что начинается url, то там нужно будет поменять.

В xml-комментах и в description для swagger'а пока просто заглушки, 
со временем можно будет туда написать что-то содержательное.

Ручка show-messages пишет в базу запрос, потом ждёт секунду (в dev окружении 10 секунд)
и выдаёт значение поля answer из той же строки. Мб не слишком умно, но пока так

Очень надеюсь, что не будет проблем с подключением к базе данных...

4) даты - пока не актуально, но потом надо будет подумать (+ обработка неправильных)

В будущем надо будет дописать как минимум ручку с выводом списка тг-каналов с номерами.
